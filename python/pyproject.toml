[project]
name = "tz-benchmark"
version = "0.1.0"
description = "Add your description here"
readme = "../README.md"
requires-python = "==3.14.*"

dependencies = [
    "citiespy==0.6.13",
    "timezonefinder==8.2.5",
    "tzfpy==1.3.3",
    "pytest",
    "pytest-benchmark",
    "pytest-pretty",
]

# [[tool.uv.index]]
# name = "tzfpy-mirror"
# url = "https://ringsaturn.github.io/tzfpy/simple"
# explicit = true

# [tool.uv.sources]
# tzfpy = { index = "tzfpy-mirror" }

# Development: unreleased tzfpy v2 (tzf-rs v2 .tzb runtime) built locally from
# the sibling checkout via `uv build --wheel`; the version string is still 1.3.3.
[tool.uv.sources]
tzfpy = { path = "../tmp/tzfpy-1.3.3-cp310-abi3-macosx_11_0_arm64.whl" }
